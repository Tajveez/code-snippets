function staircase($n) {
    // Write your code here
    $space = " ";
    $stair = "#";
    foreach(range(1, $n) as $i) {
        print(str_repeat($space, $n - $i).str_repeat($stair, $i)."\n");
    }
}
